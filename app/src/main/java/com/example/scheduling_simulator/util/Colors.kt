package com.example.scheduling_simulator.util

import android.graphics.Color

object Colors {
    val colors = arrayOf(
        Color.RED , Color.BLUE, Color.GREEN, Color.YELLOW, Color.CYAN, Color.TRANSPARENT
    )
    val size
        get() = colors.size
}